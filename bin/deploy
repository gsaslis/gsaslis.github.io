#!/usr/bin/env sh

# Run this script to deploy the app to Github Pages.

# Exit if any subcommand fails.
set -e

echo "Started deploying"

# Checkout pages branch.
if [ `git branch | grep pages` ]
then
  git branch -D pages
fi
git checkout -b pages

# Build site.
yarn install --modules-folder ./_assets/yarn
bundle exec jekyll build

# Delete and move files.
find . -maxdepth 1 ! -name '_site' ! -name '.git' ! -name '.gitignore' -exec rm -rf {} \;
mv _site/* .
rm -R _site/

# Push to pages.
git add --force --all
git commit --allow-empty --message "$(git log -1 --pretty=%B) [ci skip]"
git push --force --quiet rad pages

rad sync -a

curl https://grebedoc.dev/ -X PUT -H "Host: yorgos.net.gr" --data "https://yorgos.radicle.garden/z4L41aRs71Hvfao3DXkP9B9XbYe9K.git"


# Move back to previous branch.
git checkout -
yarn install --modules-folder ./_assets/yarn

echo "Deployed Successfully!"

exit 0
